export function generateWaitingDestinationMessage(origin: string): string {
  return `🏙 Город отправления: ${origin}\n\nУкажите пункт назначения (город):`;
}
