export const enum UserStageEnum {
  IDLE = 'idle',
  WAITING_ORIGIN = 'waiting_origin',
  WAITING_DESTINATION = 'waiting_destination',
  WAITING_DATE = 'waiting_date',
  WAITING_DATE_RANGE = 'waiting_date_range',
  CONFIRM = 'confirm',
}
